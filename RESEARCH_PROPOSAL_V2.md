# Research Proposal v2：Demo → Constraint Programs（示范编译为约束程序）

- 日期：2026-07-29
- 状态：v2 草案，**取代 v1（`RESEARCH_PROPOSAL.md`，2026-07-26）的执行策略**；方法主张与 v1 同源，路线重排
- 投递目标：**RSS 2027**（主）；ICLR 2027 不再作为本线目标（时间与形态均不匹配，见 §1.3）
- 本轮决策（老板 2026-07-29 拍板）：**Phase 0 先不动仿真机器人，只做「demo 视频理解」harness**，在 5090 服务器上以 4 个典型任务闭环；执行与冻结协议实验后置到 Phase 1/2
- 关联：`DIRECTION_AUDIT_20260726.md`（竞品与占位审计）、`PROGRESS.md`（实验总账，全部数字的权威出处）、`schema/constraint_graph_schema.md`（图 schema v0.2）

---

## 0. 一句话主张与北极星

> **一段 demo 教的是「每个阶段什么必须成立」，不是「照抄哪条轨迹」。** 我们把 demo 视频编译成一份带 typed holes 的约束程序：关系与阶段结构来自视频，一切度量数值留洞、由执行期感知现场绑定；同一条约束同时生成动作决策和阶段验收；冻结后跨场景（跨本体）复用。

北极星指标（两层，对应两个 Phase）：

- **理解层（Phase 0）**：对典型任务，自动提取的约束程序对人工金标的约束查全/查准、阶段边界 F1、歧义对区分率（§5.4）。
- **效果层（Phase 1/2，沿用 v1）**：策略代码冻结后，在 held-out seed/layout 上的成功率与泛化 gap；代码中零场景度量字面量。

---

## 1. Background

### 1.1 五条相邻路线与我们的位置

| 路线 | 代表 | 他们做什么 | 我们的差异 |
|---|---|---|---|
| 轨迹模仿 / 人视频转机器人数据 | HOST、EgoEngine、Do-As-I-Do、Qwen-RobotManip | 提取/重定向 EEF 或手部轨迹，喂 policy 训练 | 我们**不提取度量轨迹**（遮挡恰在关键帧、trajectory-frame offset 仍是度量绑定、跨 embodiment 不迁移）；只保留轨迹的形状类别与轴向（§4.6） |
| 约束表示 | ReKep、CoPa、OmniManip、KALM | 当前场景+语言 → VLM 即兴生成关键点/空间约束，每集 VLM 在环 | 约束**从 demo 视频来**（歧义/技巧/容差是常识给不了的）；**一次编译冻结复用**；约束双消费者（动作+验收） |
| Code-as-Policy | CaP、CaP-X、ASPIRE | LLM 写策略代码 + 技能库 | CaP-X 反模式已被我们证伪（度量常数零校验，B7）；我们的代码里度量值**不可表达**，只能留洞 |
| Agent-in-the-loop | VIA（2607.11119） | 现成 agent 通过 3D 界面每集在环操作，零样本 88% | 我们**摊销**：编译一次，执行无 LLM（VIA $4–15/集、≤1h/集）；成本表进论文 |
| VLA / BC | pi 系、GR00T | 采数据训练端到端 | 不采机器人数据；几何约束对视觉漂移天然稳健；可审计 |

**最近邻单点**：Semantic-Geometric Task Graph（2601.11460，人类演示→语义几何图）。差异必须写在脸上：我们输出**可执行的 typed 程序**（非图表示）、冻结跨 seed 协议、约束双消费者、（Phase 1+）跨 embodiment 实验。

### 1.2 我们的独家证据（全部有产物路径，见 `PROGRESS.md` 附录）

- **B7**：注入物理荒谬的度量常数（±0.30 m > 架半宽 0.108 m），8 份生成代码 0 拒绝 0 修正——LLM 对数字**零物理校验，只照搬**。
- **slotgeom**：历史判「成功」的放置实测距最近孔 16.7–18.0 mm = 容差 0.94 mm 的 17.8–19.1 倍——**假成功**真实存在。
- **B4-probe**：空断言代码 70/70 步跑完、场景零变化——**执行完整 ≠ 任务成功**，验收必须独立于执行。
- **B5.1**：图给关系 + harness 给感知能力时 5/8（其余组 0/8，Fisher p=0.0256）——**关系×能力交互**，二者缺一不可。
- 机制发现：转正靠**重力 + 抓点在质心上方**，主动腕转反而把 2.8° 打坏成 53.4°——demo 里可见、常识想不出的「技巧类知识」的实例。

### 1.3 三个必然攻击点与防线（本提案的任务选择与实验设计由此倒推）

1. **「碗叠碗要对中心，frontier 模型常识就知道，要 demo 干嘛？」** → demo 的价值只在常识不够用处：**歧义**（投哪个槽/推到什么朝向/进哪个孔）、**技巧**（重力转正、抓质心上方）、**容差**（什么算对齐）、**恢复结构**。防线 = 任务套件按此挑选 + **歧义对实验**（§5.4：同指令双 demo，系统必须各学各的；no-demo 基线构造性不可能做对）。
2. **「ReKep/CoPa 已经做了约束」** → 四条差异逐条消融：demo 源 / 编译摊销与冻结 / 双消费者 / 下游反推抓取（§4.2 第 3 层）。
3. **「约束代数不难，难的是 metric binding」** → 承认，并把它做成方法：两级 ReAct 一等公民（§4.4），主结果任务选 **cm 级容差**族（仿真物理可信），亚毫米插入只作 case study。

---

## 2. 主张与假设

- **H1（效果，沿 v1）**：非度量约束程序 + 运行时绑定，冻结后 held-out 成功率显著优于含度量常数的 CaP-X 风格基线（S_D≈S_E vs 崩塌）。
- **H2（理解，Phase 0 主假设）**：从 demo 视频可以稳定提取出「阶段 × {约束, 验收, 洞}」三元组：对金标 recall ≥ 0.8 / precision ≥ 0.7，歧义对区分 ≥ 3/4，自一致性 ≥ 0.8（阈值属先验目标，首轮实测后允许修订但须留痕）。
- **H3'（执行，取代 v1 的伺服闸门）**：两级 ReAct——阶段间 gate（验收不过不放行）+ 阶段内有界修正（残差=约束-现状）——相对「无 gate 直通」显著提高 held-out 成功率并缩短失败归因链。伺服（连续闭环）只是阶段内修正的一个档位（§4.5），不再单独立闸门。

---

## 3. 方法（High level）

```text
demo 视频(机器人或人手第一视角) + 一句任务指令
  → ① 拆阶段(抓取/提起/转正/对准/插入/释放…)
  → ② 每阶段提取三元组:
        约束   = 必须保持/达成的几何关系(只有关系,没有数值)
        验收   = 该阶段完成的判据(与约束同源)
        洞     = 执行时才需要的数值清单(孔位/抓取位姿/轴向…,标注类型与求解器)
  → ③ 编译成一份固定 Python policy(LLM 只在此出现一次)
  → ④ 执行: 每阶段 [感知填洞 → 动作 → 验收自检]
        过 gate → 下一阶段
        差一点 → 阶段内修正(残差有界控制,含 pose-in-hand)
        离谱   → 回退重来(不放行进下一阶段)
  → 冻结代码,换 seed/layout 只有感知返回值变化
```

编译产物形态（示意，insert_tubes）：

```python
GRAPH = [
  Node("抓取", 约束=["夹爪轴∥管长轴","抓点在质心上方"],
        洞={"抓取位姿": solve("graspnet候选→约束过滤三层漏斗")},
        验收="闭合后提起2cm物体跟随"),
  Node("转正", 约束=["管轴→竖直"], 洞={},        # 重力+抓点位置自动转正,无需数值
        验收="管轴与竖直夹角<15°"),
  Node("对准", 约束=["管轴对齐某空孔轴"],
        洞={"目标孔位": solve("架子点云拟合→空孔检测")},   # 点云拟合是求解器实例之一,可替换
        验收="水平偏差<由孔径推导的阈值(非手调)"),
  Node("插入", 约束=["沿孔轴下插,保持竖直"],
        洞={"停止条件": solve("深度残差")},
        验收="深度比例≥0.4 且竖直"),
]
```

## 4. 方法（Low level）

### 4.1 封闭约束词表 v0（提取只允许从这里选，禁自由发挥）

`axis_parallel(a,b)` / `axis_vertical(a)` / `center_align(a,b)` / `region_grasp(obj, region∈{顶部,中上,中部,底部,边沿,把手})` / `approach_direction(锥∈{顶抓,侧抓,斜抓})` / `above(a,b)` / `inside(a,b)` / `order(阶段序)` / `carry(搬运中保持的关系)` / `clearance(a,b)`。

每条约束 = `{name, args(对象/轴/区域为符号引用), holes(数值槽), provenance=demo_video, evidence_frames, confidence}`。schema 沿用 `schema/constraint_graph_schema.md` v0.2，词表以扩展字段进入。**图中出现任何世界坐标度量字面量即校验失败**（T3 扫描器接入）。

### 4.2 文字→数值的桥：抓取候选三层漏斗

原则：**文字永远不直接变数值；文字变成「对数值的检验函数」，数值只从感知/生成器来。**

```text
GraspNet 出候选(每个候选=位置/接近方向/闭合轴,数值)
  第1层 硬可行性(一票否决): IK可达 ∧ 无碰撞 ∧ 开口宽度合法
  第2层 demo几何谓词(粗标签编译成检验函数):
        grasp_height_fraction(c,tube_axis) ≥ 0.5      # "中上部"
        angle(c.approach, −z) < 30°                    # "顶抓"
        angle(c.closing_axis, tube_axis) > 60°         # "闭合⊥长轴"
  第3层 下游约束反推(新颖点):
        insertion_clearance(c)   # 抓太低→插到底夹爪撞架
        reorient_feasible(c,com) # 抓点须在质心上方,重力才转得正
  幸存者≤3 → VLM tie-break(要求引用约束说理,全程留痕)
```

纪律：谓词阈值只准从通用几何推导或全任务共用一套，**禁止 per-task 手调**；VLM 不得输出数值、不得在>3 候选中自由挑。

### 4.3 Typed holes 与求解器登记表

每洞 = `{类型(pose_se3/axis_3d/point_3d/scalar), 坐标系, 合法搜索域, 求解器, 验证方式}`。求解器是登记制、可替换：GraspNet（抓取位姿）、点云几何拟合（碗心/孔位/轴向——点云拟合只是实例，同一接口可换成检测器或 VLM 点选+反投影）、深度/力残差（停止条件）。

### 4.4 两级 ReAct（一等公民）

- **阶段间 gate**：验收谓词不过 → 不放行。离谱失败（抓反、掉落）→ 回退到指定阶段重来；gate 判据与约束同源（B4 教训：不得以「跑完没报错」冒充成功）。
- **阶段内修正**：残差 = 约束目标 − 感知现状（例：in-hand 管轴与竖直差 8° → 腕部补 8°），修正有界、带预算；demo 的作用正是给出「修正的目标是什么」，模型不必凭空想。

### 4.5 绑定档位与失败升档（包装件①）

每条约束标注绑定档位：**静态**（demo/先验即定，如阶段顺序）/ **入口绑定**（阶段开始感知一次，如孔位）/ **连续绑定**（边动边看，如插入深度）。编译器按可观测性派发默认档；held-out 上同一约束反复挂 gate → 自动升档并记录。失败归因由此获得具体产出（换档），而非笼统 retry。止损：若升档规则学不出，退化为固定派发表（仍是贡献）。

### 4.6 运动模式蒸馏（包装件②，可砍）

阶段间轨迹段（CoTracker 轨迹点即可，无需度量级）分类到小词表：`直线接近(沿轴)/弧线转正/提升-平移-下放/螺旋推压`，轴向留洞。两个消费方式：给执行选 motion primitive；给约束提取投票（「下插段沿竖直轴直线」即「沿孔轴插入」的证据）。仿真 demo 的 GT EEF 轨迹免费——只用于**验证蒸馏质量**，不作方法输入。审稿人问「轨迹信息被你扔了？」的标准答案：**保留形状类别与轴向，丢弃度量值**。

### 4.7 反事实约束验证（包装件③，Phase 1 起，需仿真）

每条候选约束入图前在仿真里故意违反一次：斜 20° 插真失败 → 约束为真入图并附实验记录；违反后仍成功 → 伪约束丢弃。demo 中「从未被违反的隐形约束」（从未倾过 15°、从未贴近架沿）同法挖掘。Phase 0 先做无仿真的降级版：反事实 QA（§5.3-V4）。

### 4.8 VLM 使用纪律

- 编译期 VLM = **Claude Opus**（`claude-opus-4-8`，模型可换，接口留开关）；仅两个合法工位：关键帧→粗标签/结构化三元组（低温、JSON schema 约束输出、逐条 evidence 帧）、三层漏斗后的 tie-break。
- 自一致性：同输入采样 k=5，按多数票入图，分歧项降 confidence 并标记人审。
- 全部调用缓存（帧 hash+prompt hash）、成本入账。执行期 runtime **无 LLM**。

---

## 5. Phase 0：Demo 理解 Harness（当前唯一开工项，不碰仿真）

### 5.1 目标与范围

在 5090 服务器上，对 4 个典型任务把「视频 → 阶段 × {约束,验收,洞} → 校验 → 人审报告 → 对金标打分」整条链跑成**可重复的 CLI**。不做执行、不做仿真、不做训练。

任务四件套（按 §1.3 攻击点-1 的「demo 增量」原则挑选）：

| 任务 | demo 比常识多教了什么 |
|---|---|
| `insert_tubes` | 技巧（抓质心上方→重力转正）+ 哪根管进哪个孔 + 对准容差 |
| `stack_bowls` | 关系放置基线（常识占比高，作为「demo 增益≈0」的对照点） |
| `deposit_coin` | 歧义主战场：投哪个槽语言定不了 |
| `push_T` | 目标位姿歧义：推到什么朝向/位置由 demo 定义 |

素材：复用 wht `robot-subtask-seg` 的 demo 视频与 refined traces（**只读 upstream，source manifest 登记路径+mtime**）；`push_T_random` 等 random 变体用于构造歧义对。

### 5.2 架构与目录

```text
harness/
  cli.py          # ingest / extract / validate / report / metrics 五个子命令
  ingest.py       # 视频→帧抽样+元数据; 优先复用已有 trace 的阶段边界
  stages.py       # 阶段切分(复用 trace; 缺则 Opus 提议+人审确认)
  keyframes.py    # 关键事件帧: 接触/闭合/提起/转正/预对准/插入/释放
  extract.py      # Opus 结构化提取(词表约束); k=5 自一致性投票
  validate.py     # schema 校验 + T3 度量字面量扫描 + 跨帧一致性
  report.py       # 每任务 report.html: 关键帧+图+逐条约束人审勾选(金标标注同一界面)
  metrics.py      # 对金标: 约束P/R、阶段F1、洞识别、歧义对区分、自一致性
  vocab.py        # §4.1 封闭词表(代码即规范)
  prompts/        # stage_split.md / constraint_extract.md(版本化,改动走 git)
  goldset/        # 每任务人工金标 JSON(PI+学生经 report.html 标注)
  runs/           # 每次提取的产物+成本日志(不进 git 的大文件按 .gitignore)
```

### 5.3 校验四层（Phase 0 无仿真时「理解质量」如何量）

- V1 结构：schema + 词表合法性 + 零度量字面量（自动）。
- V2 稳定：k=5 自一致性、同视频重复运行一致率（自动）。
- V3 金标：4 任务金标图（预计每任务 30–60 min 人工），约束 P/R、阶段 F1、洞识别（半自动）。
- V4 反事实 QA（无仿真降级版）：对每条约束生成违反情形描述/合成图，问模型「任务还能成吗」，与金标逻辑对照（自动+抽审）。

### 5.4 验收门（Phase 0 完成判据）

金标 4/4 齐；对金标 recall ≥0.8 / precision ≥0.7；**歧义对区分 ≥3/4**（同指令、不同 demo → 提取出可判别差异的约束集）；自一致性 ≥0.8；零度量字面量违例；单任务端到端成本 ≤ $5、可缓存复跑。未达门槛 → 先修词表与提示词，两轮仍不达 → 收缩任务集并如实记录。

### 5.5 明确不做（Phase 0）

不接仿真执行；不做完整 6-DoF EEF 轨迹提取（§4.6 蒸馏版都后置）；不训任何模型；不动 1022/1023 与学生工作区（只读拉素材）。

---

## 6. Phase 1/2 与实验矩阵（预告，细化另文）

- **Phase 1（执行绑定，5090 `knowin_sim_v2`）**：最小执行层对接 knowin-world；三层漏斗实装（wht `grasp_candidate_filter` 为第 1 层底座）；两级 ReAct；反事实法庭上线。
- **Phase 2（冻结协议主实验）**：D/E seed 协议（T4 已有链路）；对照组沿 v1 六组，另加两条：**no-demo frontier agent**（VIA 式同 API，对攻击点-1）与 **per-episode VLM 约束**（ReKep 式，对攻击点-2）；成本/延迟摊销表；counterfactual 抓取场景（局部最优 vs 下游可行）。
- 里程碑与止损沿 `RESEARCH_MILESTONES.md` 框架按 RSS 2027 倒排（另文更新）。

## 7. Infra 与工作方式（2026-07-29 起）

- **5090 服务器 = 共同实验机**：`knowin-sim@192.168.77.113`（key 登录已配好）。RTX 5090 32GB、磁盘 1.7T 空、py3.12；`/home/knowin-sim/knowin_sim_v2`（knowin-world + data，Phase 1 用，**不迁移**）、`knowin_sim_v4verify` 备用。
- **代码同步拓扑（2026-07-29 定稿）**：主仓 = 内网 Gitea `http://192.168.20.77/muz1lee/demo-graph-lab`（**私有**）。mac 编辑/commit → `git push gitea main` → 5090 拉取（mac agent-forward 驱动 `git pull`，rsync 兜底）。GitHub 远端保留为历史备份，不再作为同步枢纽。
- **1022/学生区**：保持只读 upstream；素材经 source manifest 登记后拉到 5090 `data/upstream/`。
- 安全提示：5090 密码过弱且我方已配 key，建议尽快禁用密码登录或改强密码；Anthropic API key 需放服务器 `.env`（**由老板自己放或明确授权我从本地拷**，本提案不擅自迁移密钥）。

## 8. TODO

**本周（Claude 主执行）**
1. harness 骨架落码并在 `insert_tubes` 上端到端跑通 ingest→extract→validate→report（Opus 真调用、含成本日志）。
2. 5090 环境：uv（用户态）+ venv + opencv/imageio（ffmpeg 用户态方案）；仓库落位 `/home/knowin-sim/demo-graph-lab`。
3. 从 1022 拉 4 任务素材（视频 + refined traces + demonstration_bundle.json）至 5090 `data/upstream/`，写 source manifest。
4. report.html 第一版（含金标标注勾选）。

**本周（老板）**
5. 审本提案；定词表 v0 终版；给 4 任务各写一句「demo 比常识多教了什么」终稿（§5.1 表为草稿）。
6. ANTHROPIC_API_KEY 放 5090（或授权迁移）。（同步已切 Gitea 私有仓，无需 GitHub deploy key。）

**两周内**
7. 金标 4/4 标注完成（report.html 内完成，预计每任务 ≤1h）。
8. 第一轮全量提取 + 指标报告；歧义对（`push_T` vs `push_T_random`、deposit_coin 双槽）第一轮。
9. 按首轮结果修订 §5.4 阈值并留痕。

**一月内**
10. Phase 0 验收门裁决 → 启动 Phase 1（最小执行层设计另文）；RSS 2027 倒排里程碑更新进 `RESEARCH_MILESTONES.md`。

**学生（不新增负担）**
- wht：继续执行侧；仅需一次性指认 4 任务素材的权威路径。
- zyh：不参与本线；skill-library 第二篇边界另约时间对齐。

---

## 附录 A：与 v1 的差异清单

| 项 | v1（07-26） | v2（本文） |
|---|---|---|
| 首要工作面 | M1 单管执行（仿真） | **Phase 0 视频理解 harness（无仿真）** |
| 伺服/H3 | 独立假设+闸门 | 并入两级 ReAct 的「连续绑定」档位（§4.4/4.5） |
| 投递 | RSS/ICRA 2027（后被 ICLR 讨论覆盖） | **RSS 2027**（定） |
| 新增 | — | 歧义对实验、三层漏斗纪律化、绑定档位、运动模式蒸馏、反事实法庭、no-demo/per-episode 两条新基线、理解层北极星 |
| 证据索引 | 附录 A | 沿用 v1 附录 A 与 `PROGRESS.md`，不重复维护 |
