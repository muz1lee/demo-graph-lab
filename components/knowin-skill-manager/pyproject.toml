[project]
name = "knowin-skill-manager"
version = "0.1.0"
description = "Prototype manager for generated knowin-world YAML skills"
requires-python = ">=3.10"
dependencies = [
  "PyYAML",
]

[project.scripts]
ksm = "ksm.cli:main"

